export default function Dashboard() {
    return (
        <div>
            This will be the dashboard for logged in users. <br />
            Those with permissions will be able to update the database.
        </div>
    );
}
